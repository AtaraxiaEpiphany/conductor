# Project Purpose

> The wiki's directional intent — *why* this project exists and where it is heading.
> Distinct from the schema (how the wiki is structured) and the overview (what it is today):
> purpose is the evolving thesis. Maintained by wiki-synthesizer Phase 2 from track specs + decisions.
> Last updated: {TIMESTAMP}

## Goals

{2–4 concise, durable project goals. What success looks like over the project's life — not per-track tasks.}

> **Lifecycle:** seeded by `/conductor:setup` §2.1 from the project's stated goal,
> then refined by `/conductor:wiki purpose`. Stable across tracks.

## Key Questions

{Open questions the project is investigating. As a track answers one, move its resolution into the Thesis and strike it here.}

> **Lifecycle:** placeholder until the first track runs. Seeded by
> `/conductor:new-track` from the first Brief's `## Open Questions` (additive —
> only questions not already present); thereafter maintained by
> `/conductor:wiki purpose` and wiki-synthesizer as tracks surface new questions.

## Evolving Thesis

{The current synthesized understanding of the project's direction, accumulated from completed tracks and technical decisions. Updated each time wiki-synthesizer runs; contradictions between tracks are surfaced here, not hidden.}

> **Lifecycle:** deliberately empty at setup — it is a SYNTHESIS output, not a
> seed. Filled incrementally by wiki-synthesizer Phase 2 as tracks complete and
> their spec/decision blocks are harvested. A populated Thesis on day one would
> be fabrication.

## In Scope

{What this project deliberately covers — boundaries that persist across tracks.}

> **Lifecycle:** seeded by `/conductor:setup` from the project description;
> refined by `/conductor:wiki purpose`. Persists across tracks.

## Out of Scope

{What this project deliberately does NOT cover — settled exclusions so a future track does not re-litigate them.}

> **Lifecycle:** placeholder until the first track runs. Seeded by
> `/conductor:new-track` from the first Brief's `## Out of Scope` (additive,
> intersection-only — never widens past what setup established); thereafter each
> Brief's Out-of-Scope is intersected against this, and wiki-synthesizer
> promotes settled exclusions here.

## Active Decisions

{Architecture/design decisions that shaped direction, accumulated from track specs and the `## Technical Decision:` blocks harvested by wiki-synthesizer. Each entry: decision + one-line rationale + `[[wikilink]]` to the source.}

> **Lifecycle:** deliberately empty at setup — decisions are harvested from
> completed tracks, not invented up front. Filled by wiki-synthesizer Phase 2
> from `## Technical Decision:` blocks in specs and the cross-phase
> `track-findings` compiler.
