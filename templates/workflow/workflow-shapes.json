{
  "_comment": "Single source of truth for workflow SHAPE (the second axis). The task-type registry says what each node SAYS (routing + workflow prose); THIS registry says the node SEQUENCE — which agents a workflow runs, in what order, its verify policy, its stop condition, AND which checkpoint verifiers it fans out. The shape is PARTIALLY LOAD-BEARING: the `verifiers` field drives the checkpoint fan-out (dispatch.py cmd_dispatch_next + _step_emit_dispatch_batch iterate verifiers_for(shape)), so a project shape omitting test-runner simply doesn't fan it out. The `nodes` topology STAYS ADVISORY: shape_allows (dispatch.py) consults it only to attach a `shape_violation` disclosure when an action's agent is off-topology — never to block or reroute — and the planner→executor→checker spine runs unchanged regardless of the resolved shape. (nodes is the SPINE topology; verifiers are its checkpoint CHILDREN — distinct lists, never conflated: a phase-checker dispatching a verifier is on-topology even though the verifier is not in nodes.) The other accessors (verify_policy_for / stop_condition_for / instruction_for) feed `registry-doc --shape` display ONLY — `instruction_for` is NOT injected into an orchestrator prompt (contrast the task-type `workflow` field, which IS). So setting `research-first` surfaces `shape_violation` disclosures but does NOT run explorer first. (A project may still overlay conductor/workflow/workflow-shapes.json — opt-in by file presence — to add or override shapes with zero plugin edits; the rows resolve, render, and — for the verifier axis — drive the fan-out.)",
  "_fields": {
    "nodes": "the ordered list of SPINE dispatch agents this shape runs (the topology). Action tokens: spec-planner | explorer | task-executor | phase-checker. ADVISORY: shape_allows consults this only to surface a `shape_violation` disclosure when an off-topology agent dispatches — it does NOT block. Checkpoint verifiers (ac-tracer / test-runner) are NOT spine nodes; they are checkpoint children declared in `verifiers` below, and a phase-checker dispatching one is on-topology regardless of `nodes`.",
    "verifiers": "the ordered verifier-agent list fanned out at this shape's checkpoint (checkpoint CHILDREN of the spine — distinct from `nodes`, the spine topology). Each entry must be a verifier name (`ac-tracer` or `test-runner`). The fan-out emits one wave member per entry (the per-member envelope built inline by `_build_verifier`). Absent/empty => the standard [ac-tracer, test-runner] pair (fail-open). Unknown entries are dropped at fan-out. This is the load-bearing seam that makes a shape control WHICH verifiers its checkpoint runs.",
    "requires": "optional prerequisites the shape demands of track-state before it can run (advisory; future hook). Absent => no prerequisites.",
    "verify_policy": "how this shape gates progress: checkpoint (the default — phase-checker stamps a checkpoint gate) | none (no checkpoint gate — research/exploration shapes that produce no committable artifact).",
    "stop_condition": "what makes the shape DONE: all_nodes_done (every node in `nodes` has run) | a structured pass_condition. The fixed spine's default is all_nodes_done.",
    "instruction": "optional prompt-shaping prose for the orchestrator when this shape is active (mirrors the task-type `workflow` field). Absent => the orchestrator follows the default §3.0 dispatch loop."
  },
  "default": {
    "nodes": ["spec-planner", "task-executor", "phase-checker"],
    "verifiers": ["ac-tracer", "test-runner"],
    "verify_policy": "checkpoint",
    "stop_condition": "all_nodes_done"
  },
  "shapes": {
    "default": {
      "nodes": ["spec-planner", "task-executor", "phase-checker"],
      "verifiers": ["ac-tracer", "test-runner"],
      "verify_policy": "checkpoint",
      "stop_condition": "all_nodes_done",
      "instruction": "The standard implement loop: spec-planner decomposes the brief into a plan, task-executor implements each task under the TDD/coverage gates, phase-checker verifies each phase and stamps the checkpoint. This is the shape the conductor has always run — declared here so it is data-driven, not hardcoded."
    },
    "research-first": {
      "nodes": ["explorer", "spec-planner", "task-executor"],
      "verifiers": ["ac-tracer", "test-runner"],
      "verify_policy": "none",
      "stop_condition": "all_nodes_done",
      "when_to_use": "Declares the INTENDED topology for a research-heavy track — one whose first need is understanding (map the codebase / survey a dependency / spike an approach) before planning. ADVISORY: the conductor does NOT yet reorder dispatch by shape — today both shapes plan first, and this row documents intent + surfaces drift via `shape_violation` (e.g. when an `explorer` dispatch lands on a track whose shape omits it, or vice-versa). Pick this shape to RECORD that exploration should precede planning; making the conductor actually honor that ordering is a future code change at the dispatch emit site, not a registry edit.",
      "instruction": "ADVISORY — not injected today. Records the intent that, for a research-heavy track, explorer should run before spec-planner so the plan is decomposed with exploration findings in hand, and that no phase-checker checkpoint gates exploration (it produces no committable artifact). This prose is rendered by `registry-doc --shape research-first` for human/tooling reference; it is NOT injected into the orchestrator prompt, and the dispatch spine does not reorder to honor it. The shape surfaces a `shape_violation` disclosure if the agents actually dispatched drift off the declared `nodes`, so the misconfiguration is visible rather than silent."
    }
  }
}
