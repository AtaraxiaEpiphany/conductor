<!-- conductor:toc begin -->
# Conductor

## File Index

**Creation map** — where new documents go. Use this when an explicit path is
missing; all new documents MUST be created at the following paths. For *read
strategy* (which docs to read in full vs. on-demand), see `conductor/index.md` —
the two list the same first-class docs but group them differently.

Each row's **Creation Rule** begins with a status tag (the same vocabulary
`conductor/index.md` uses): **seeded** (setup creates it), **auto** (a skill
creates it on first use), or **on-demand** (you or an agent create it when
needed).

| Category        | Document Type           | Default Path Pattern                                       | Creation Rule                                                                 |
| :-------------- | :---------------------- | :--------------------------------------------------------- | :---------------------------------------------------------------------------- |
| **Product**     | Product Definition      | `./conductor/product/product.md`                           | **seeded** — create if missing.                                               |
|                 | Product Guidelines      | `./conductor/product/product-guidelines.md`                | **seeded** — create if missing.                                               |
| **Requirement** | PRD                     | `./conductor/requirement/prd/<name>.md`                    | **on-demand** — create here if missing.                                       |
|                 | UX/UI Spec              | `./conductor/requirement/ux-ui/design-spec.md`             | **on-demand** — create if missing.                                            |
| **Design**      | Tech Stack              | `./conductor/design/tech-stack.md`                         | **seeded** — global; create if missing. Frameworks, languages, versions. Read in full. |
|                 | Architecture            | `./conductor/design/architecture/system-architecture.md`   | **auto** — scoped; seeded by corpus-writer (post-track). Do not hand-create.  |
|                 | DB Schema               | `./conductor/design/database/schema.md`                    | **auto** — seeded by corpus-writer (post-track).                              |
|                 | API Specs               | `./conductor/design/api-specs/<endpoint>.md`               | **auto** — seeded by corpus-writer (post-track). Strict schema adherence required. |
|                 | Decision Records        | `./conductor/design/decision-*.md`                         | **on-demand** — ADR-style; append, never delete.                              |
|                 | Doc Conventions         | *(plugin-provided)*                    | **seeded** — authoring rules live in the Conductor plugin's `doc-conventions.md` (loaded on demand by corpus-writer/wiki-synthesizer/doc-linter/wiki) — not a project file; do not hand-create. |
| **Wiki**        | Wiki Overview           | `./conductor/overview.md`                                  | **seeded** — auto-regenerated by wiki-synthesizer; do not edit.               |
|                 | Wiki Purpose            | `./conductor/purpose.md`                                   | **seeded** — create if missing. Co-evolved (user owns Goals/Scope).           |
|                 | Wiki Log                | `./conductor/log.md`                                       | **seeded** — append-only.                                                     |
|                 | Wiki Queries            | `./conductor/queries/<slug>.md`                            | **auto** — filed by `/conductor:wiki query`.                                  |
| **Workflow**    | Workflow Index          | `./conductor/workflow/index.md`                            | **seeded** — create if missing.                                               |
|                 | Code Patterns           | `./conductor/workflow/code-styleguides/<code-patterns>.md` | **on-demand** — create if missing.                                            |
|                 | Code Style              | `./conductor/workflow/code-styleguides/<language>.md`      | **seeded** — create if missing.                                               |
|                 | Dev Commands            | `./conductor/workflow/dev-commands/<language>.md`          | **seeded** — create if missing.                                               |
|                 | Git Flow                | `./conductor/workflow/git-flow.md`                         | **on-demand** — create if missing.                                            |
|                 | Testing                 | `./conductor/workflow/testing/strategy.md`                 | **seeded** — create if missing.                                               |
| **Resources**   | Glossary                | `./conductor/resource/glossary.md`                         | **on-demand** — global; create if missing.                                    |
|                 | References              | `./conductor/resource/references/index.md`                 | **on-demand** — create if needed.                                             |
|                 | FAQ                     | `./conductor/resource/faq/index.md`                        | **on-demand** — create if needed.                                             |
|                 | Graduated Findings      | `./conductor/resource/<topic>.md`                          | **auto** — seeded by corpus-writer (inventory/gotcha/facts).                  |
| **Management**  | Tracks Registry         | `./conductor/tracks.md`                                    | **seeded** — create if missing.                                               |
|                 | Track Spec/Plan/Meta    | `./conductor/tracks/<track_id>/`                           | **auto** — created by `/conductor:new-track`; read/update based on context.   |
<!-- conductor:toc end -->
